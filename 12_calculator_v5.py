# Introducing the round() function:

# Ask the user for a couple of numbers but now assume it can use a float number

x = float(input("What's x? "))
y = float(input("What's y? "))

z = round(x + y)
print(z)